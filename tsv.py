if args.context == "database":
    if args.action == "import":
        with gzip.open ('/data/title.basics.tsv.gzip') as tsvfile
        csv.Dictreader(tsvfile, delimiter = "\t", quoting = csv.QUOTE-NONE)
        for row in rows : 
            print (row)













movie = findMoviebyImdb(tconst)
for director in directors:
    person = findPersonByImdbId(dicrector_nconst)
    movie_person = {
'movie_id' :movie.id,
'person_id': person.id,
'role': writer}
movies_people.append(movie_person)


for row in rows:
if row['isAdult'] == "1" or row['titletype'] !="movie":
continue

duration = row[
if duration == "\\N":
duration = none